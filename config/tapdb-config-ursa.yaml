meta:
  config_version: 4
  client_id: ursa
  database_name: ursa
  owner_repo_name: ursa
  domain_code: Z
  domain_registry_path: /absolute/path/to/domain_code_registry.json
  prefix_ownership_registry_path: /absolute/path/to/prefix_ownership_registry.json
  # Explicit process contract for TapDB/Meridian subprocesses:
  # MERIDIAN_DOMAIN_CODE=Z
  # TAPDB_OWNER_REPO=ursa
target:
  domain_code: Z
  engine_type: local
  host: localhost
  port: '5588'
  ui_port: '8918'
  user: postgres
  password: ''
  database: tapdb_shared_dev
  schema_name: tapdb_ursa_dev
  audit_log_euid_prefix: RGX
  support_email: support@lsmc.bio
  cognito_user_pool_id: ''
safety:
  safety_tier: local
  destructive_operations: confirm_required
